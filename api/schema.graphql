type User {
  id: String!
  geoEnabled: Boolean
  roles: [Role]!
}

type Role {
  id: String!
  name: String
  description: String
  user: User! @relation
}

type Query {
  allUsers: [User]!
  user(id: String): User
}