type User @collection(name: "users") {
  name: ID!
  theme: String
  geoEnabled: Boolean!
}

type Query {
  allUsers: [User!]! @index(name: "all_users")
}

type Mutation {
  toggleGeo(userId: ID!, toggle: Boolean!): User! @resolver(name: "toggle_geo")
}